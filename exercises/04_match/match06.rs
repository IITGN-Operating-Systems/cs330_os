
fn main() {
    let o = Some(7);

    // TODO: Remove the whole `match` block, using `if let` instead 
    match o_to_remove {
        Some(i) => {
            println!("This is a really long string and `{:?}`", i);

            println!("Success!");
        }
        _ => {}
    };
}
