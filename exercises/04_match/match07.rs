
// TODO: Fill in the blank
enum Foo {
    Bar(u8)
}

fn main() {
    let a = Foo::Bar(1);

    #[allow(irrefutable_let_patterns)]
    __ {
        println!("foobar holds the value: {}", i);

        println!("Success!");
    }
}
